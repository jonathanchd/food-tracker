#include <iostream>
#include <fstream>
#include <vector>
#include <algorithm>
using namespace std;

bool fileExists(string file);
void toLower(string& s);
string prompt(string ques);
void readFile(string file);